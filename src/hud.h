#ifndef __HUD_HEADER__
#define __HUD_HEADER__

#include "globals.h"

#define HUD_LEFT 0.0f
#define HUD_TOP 0.0f
#define HUD_RIGHT renderWidth
#define HUD_BOTTOM renderHeight

#define HUD_WIDTH renderWidth
#define HUD_HEIGHT renderHeight
#define HUD_WIDTHHALF (renderWidth * 0.5f)
#define HUD_HEIGHTHALF (renderHeight * 0.5f)
#define HUD_WIDTHQUARTER (renderWidth * 0.25f)
#define HUD_HEIGHQUARTER (renderHeight * 0.25f)

#endif